[
  {
    "comment": "Add your test case here for debugging and run go test -run=One.",
    "query": "SELECT c.id, c.name, sum(c.price) from customer c join corder o on c.id = o.cust_id group by c.id, c.name",
    "plan": {

    }
  }
]