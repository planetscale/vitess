[
  {
    "comment": "Add your test case here for debugging and run go test -run=One.",
    "query": "select count(t1.id) from user t1, music t2 group by t2.foo order by t2.foo",
    "plan": {

    }
  }
]