{
  "OperatorType": "Projection",
  "Expressions": [
    ":2 as manager_id",
    "1 as 1",
    "weight_string(:2) as weight_string(manager_id)"
  ],
  "Inputs": [
    {
      "OperatorType": "Sort",
      "Variant": "Memory",
      "OrderBy": "2 ASC",
      "Inputs": [
        {
          "OperatorType": "RecurseCTE",
          "JoinVars": {
            "cte_id": 0
          },
          "Inputs": [
            {
              "OperatorType": "Route",
              "Variant": "Scatter",
              "Keyspace": {
                "Name": "mysqltest",
                "Sharded": true
              },
              "FieldQuery": "select id, `name`, manager_id from employees where 1 != 1",
              "Query": "select id, `name`, manager_id from employees where manager_id is null",
              "Table": "employees"
            },
            {
              "OperatorType": "Route",
              "Variant": "Scatter",
              "Keyspace": {
                "Name": "mysqltest",
                "Sharded": true
              },
              "FieldQuery": "select e.id, e.`name`, e.manager_id from employees as e where 1 != 1",
              "Query": "select e.id, e.`name`, e.manager_id from employees as e where e.manager_id = :cte_id",
              "Table": "dual, employees"
            }
          ]
        }
      ]
    }
  ]
}
